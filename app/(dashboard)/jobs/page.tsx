import { PlaceholderPage } from "@/components/dashboard/placeholder";

export default function JobsPage() {
  return (
    <PlaceholderPage
      title="Jobs"
      phase="Phase 1"
      description="Create and publish jobs to your branded public careers page — title, salary, location, closing date, vacancies, and assigned application form."
    />
  );
}
