import { PlaceholderPage } from "@/components/dashboard/placeholder";

export default function OnboardingBoardPage() {
  return (
    <PlaceholderPage
      title="Onboarding"
      phase="Phase 4"
      description="Track every new starter's checklist — forms, documents, references with bounce detection, DBS, shadow shifts and induction — with automatic reminders."
    />
  );
}
