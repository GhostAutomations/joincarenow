import { PlaceholderPage } from "@/components/dashboard/placeholder";

export default function PipelinePage() {
  return (
    <PlaceholderPage
      title="Pipeline"
      phase="Phase 2"
      description="Kanban and table views of every applicant — drag between stages, filter by job, area or missing documents, with every move audit-logged."
    />
  );
}
