import { PlaceholderPage } from "@/components/dashboard/placeholder";

export default function TemplatesPage() {
  return (
    <PlaceholderPage
      title="Templates"
      phase="Phase 3"
      description="Email and SMS templates with merge fields — interview invites, reminders, offers, DBS application links and payroll notifications."
    />
  );
}
